<%=@course.name%>
<%=link_to "Create New Challenge", new_course_challenge_path(@course,@challenge)%>
<ul>
<% @challenges.each do |challenge| %>
  <li><%=link_to(challenge.title, course_challenge_path(@course, challenge)) %></li>
<%end%>
</ul>
