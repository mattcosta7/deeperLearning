<script src="//maps.google.com/maps/api/js?v=3.13&sensor=false&libraries=geometry" type="text/javascript"></script>
<script src="//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js" type="text/javascript"></script>

Find a Tutor to help you on your path!

  <div style='width: 800px;'>
    <div id="user_map" style='width: 800px; height: 400px;'></div>
  </div>

<ul>
<% @users.each do |user| %>
  <% if user != current_user %>
  <li>
    <%=user.name%> is approx. <%=user.distance_to(current_user.location).ceil%> Miles away
  </li>
  <%end%>
<%end%>
</ul>

<script type="text/javascript">
  var handler = Gmaps.build('Google');
  handler.buildMap({ internal: {id: 'user_map'}}, function(){
    var markers = handler.addMarkers(<%=raw @hash.to_json %>);
    handler.bounds.extendWith(markers);
    handler.fitMapToBounds();
  });
</script>